<div class="tags">
  タグ:
  <% Tag.all.each do |tag| %>
    <% if tag.id == params[:tag_id].to_i %>
      <span class="current_tag"><%= tag.value %></span>
    <% else %>
      <%= link_to tag.value, [ :staff, tag, :messages ] %>
    <% end %>
  <% end %>
</div>

<!-- <div class="tags">
  タグ:
  <% Tag.all.each do |tag| %>
    <% if tag.id == params[:tag_id].to_i %>
      <span class="current_tag"><%= tag.value %></span>
    <% else %>
        <% if params[:action] == 'index' %>
          <%= link_to tag.value, [ :staff, tag, :messages ] %>
        <% end %>
        <% if params[:action] == 'inbound' %>
          <%= link_to tag.value, [ :staff, tag, :messages ] %>
        <% end %>
        <% if params[:action] == 'outbound' %>
          <%= link_to tag.value, [ :staff, tag, :messages ] %>
        <% end %>
        <% if params[:action] == 'deleted' %>
          <%= link_to tag.value, [ :staff, tag, :messages ] %>
        <% end %>
    <% end %>
  <% end %>
</div> -->
